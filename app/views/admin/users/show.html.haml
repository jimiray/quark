.pull-right
  = link_to raw("<span class='fa fa-pencil'></span> Edit"), edit_admin_user_path(@user), class: 'btn btn-purple'
  = link_to raw("<span class='fa fa-trash-o'></span> Disable"), admin_user_path(@user), class: 'btn btn-danger', data: {confirm: "Are you sure?"}, method: :delete
%h3 User - #{@user.name}
